#include "main.h"

/**
 * add - computes the result of addition of two integers
 *
 * @a: left operand
 * @b: right operand
 *
 * Return: returns 0
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}

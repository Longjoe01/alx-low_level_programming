#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 *
 * int: an integer
 * @r: r is an integer
 * Return: returns an integer
 */
int _abs(int r)
{
	if (r < 0)
	{
		return (-r);
	}
	else if (r > 0)
	{
		return (r);
	}
	else 
	{
		return (r);
	}
}

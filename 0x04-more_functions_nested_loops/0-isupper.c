#include "main.h"

/**
 * _isupper - check for lowercase character
 * @c: uppercase character
 * Return: 0 if lowercase, else 1
 */
int _isupper(int c)
{
	char c;

	if (_isupper(c))

	{
		return (0);
	}
	else
	{
		return (1);
	}

	return (_isupper);
}

